define(['dispatcher', 'text/chars.store'], function(dispatcher, charsStore) {

	"use strict";

	var _handleChange = function() {
		// storeData = store.getData();
	}

	var init = function() {
		// _handleChange();

		// store.eventEmitter.subscribe(_handleChange);
	}

	return {
		init: init
	}
});